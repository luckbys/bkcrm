// DEPRECATED: Este arquivo não é mais usado
// Os departamentos agora são gerenciados pelo banco de dados via useDepartments hook
// Mantido apenas para referência histórica

import { Department } from '@/types/evolution-api';

export const departments: Department[] = [
  // Dados movidos para a tabela 'departments' no banco de dados
  // Use o hook useDepartments() para acessar os departamentos
]; 